<?xml version="1.0"?>

<!DOCTYPE module SYSTEM "../../../../dtd/module.dtd">

<module name="Module ngx_http_index_module"
        link="/en/docs/http/ngx_http_index_module.html"
        lang="en">

<section id="summary">

<para>
模块 <literal>ngx_http_index_module</literal> 处理以斜线字符(‘<literal>/</literal>’)结尾的请求。
</para>

</section>


<section id="example" name="Example Configuration">

<para>
<example>
location / {
    index index.$geo.html index.html;
}
</example>
</para>

</section>


<section id="directives" name="Directives">

<directive name="index">
<syntax><value>file</value> ...</syntax>
<default>index.html</default>
<context>http</context>
<context>server</context>
<context>location</context>

<para>
定义将要被作为索引的文件
文件 <value>file</value> 的名字可以包含变量。
文件以指定的顺序被检查。
列表中的最后一个元素可以是一个带有绝对路径的文件
例子：
<example>
index index.$geo.html index.0.html /index.html;
</example>
</para>

<para>
需要注意的地方是，如果有一个内部重定向，并且请求可以被不同的location处理
比如,下面这个例子
<example>
location = / {
    index index.html;
}

location / {
    ...
}
</example>
一个有“<literal>/</literal>”的请求将会在第二个location中以“<literal>/index.html</literal>”的形式被处理。
</para>

</directive>

</section>

</module>
